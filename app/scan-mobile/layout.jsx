export const metadata = {
  title: 'Mobile Scanner Session | SababPDF',
  description: 'Capture pages from mobile and send them to your SababPDF scanning session.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function Layout({ children }) {
  return children;
}
