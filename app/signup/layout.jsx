export const metadata = {
  title: 'Sign Up | SababPDF',
  description: 'Create a SababPDF account for a personalized experience.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function Layout({ children }) {
  return children;
}
