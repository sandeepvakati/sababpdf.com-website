export const metadata = {
  title: 'Login | SababPDF',
  description: 'Sign in to your SababPDF account to manage your profile and settings.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function Layout({ children }) {
  return children;
}
